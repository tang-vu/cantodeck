# Audio rework — development checkpoint, 2026-09-08

The goal remains the full project specification and comfortable live singing through the owner's external speakers. Neither native compilation nor a synthetic latency result is acceptance of that goal. The DGM20 is currently absent from Windows enumeration; the exact external-speaker connection is awaiting the owner's answer.

## Changes made

- Added an `AudioBackend` boundary and a direct Windows WASAPI implementation. Capture and render are serviced by one MMCSS `Pro Audio` thread. Application processing calls have no owned mutex, allocation or file I/O. Device service calls and OS scheduling are not claimed to be hard real-time.
- Negotiates `IAudioClient3` periods where supported; retries without RAW properties and then standard shared mode if necessary. Reports accepted RAW requests, actual periods and allocation capacities separately.
- Maintains one output period plus a 1 ms scheduling margin instead of filling the entire WASAPI allocation. The driver allocation itself is not presented as the queued-audio target.
- Replaced the slow, underdamped queue servo with direct proportional occupancy control. Low-latency capture target is one output quantum converted into input frames plus one capture quantum and 16 guard samples.
- Added consumer-side backlog recovery: when queued capture exceeds the target by more than four negotiated quanta (minimum margin 1024 frames), discard old queued input back to the target instead of replaying a long stale vocal segment. Diagnostics count resynchronizations and discarded frames separately from FIFO overflow. Startup, starvation and recovery use a 5 ms amplitude transition, with no additional steady-state queue. These transitions also affect the dry recording tap. Recovery deliberately loses audio and is not uninterrupted capture or latency compensation.
- Replaced linear interpolation with a precomputed 32-tap windowed-sinc phase table. It uses 16 input-frame lookahead (about 0.33 ms at 48 kHz) and a lower cutoff for downsampling. This is not a mastering-quality/universal rate-conversion claim.
- Added a transparent vocal path. Natural/Dry presets bypass coloration and effects; software gain still applies. Added a stereo-linked, zero-lookahead sample-peak limiter with 80 ms release in place of hard clipping. No true-peak or acoustic-feedback protection is claimed.
- Transparent/processed selection now crossfades linearly over 5 ms, retaining both processing states. Fixed high-pass/tone coefficients are prepared once; gain/threshold dB conversions are cached until changed. This reduces repeated calculations, but no measured CPU or hardware latency improvement is asserted.
- Added explicit user-triggered speaker-to-microphone loop measurement. It plays a short quiet shaped pseudorandom signal, temporarily pauses local music/monitoring, keeps returned samples in RAM and analyses correlation off the audio thread. It rejects silence, clipping and ambiguous returns. Its result includes acoustic travel and is not inferred from buffer sizes. It has NOT been physically validated yet.
- Saves native endpoint IDs in addition to display names, with name fallback for old sessions, plus backend/rate/buffer/channel choices. Monitoring remains off on start/reconnect.

## Evidence so far

- Local Windows/MSVC Release builds and CTest pass.
- Streaming fault-adapter tests now explicitly gate the reader after prefill, seek while it is stalled, verify zero/stale-data rejection and one wait count per callback, then release it and compare exact requested stereo samples. A synthetic 24-hour-declared reader stops reading when the eight-block queue is full (not a real 24-hour file or memory-profile test). Separate cases verify worker read-failure latching/silence and prefill-failure rejection. A reader-call thread check confirms that the tested sample calls did not invoke file-reader work on the consumer thread. Release/CTest passed 3/3; full offline/UI verification passed (`build/evidence-20260908-214418`). The gate uses explicit release with cleanup on test failure, not a timed auto-release. Actual disk faults, blocked OS I/O cancellation and audible recovery remain unverified.
- Streaming follow-up: the all-in-RAM backing player was replaced with a dedicated WAV read-ahead worker, eight 1024-frame stereo blocks, source-index-based seek recovery and fixed callback work bounds. Four blocks preload off the audio thread. Mono/stereo 8-192 kHz, at most 24 hours declared duration; this is an input bound, not a 24-hour endurance claim. Sample storage no longer scales with song length; codec metadata/OS caching remain outside that bound. Music waits preserve its position and emit silence without blocking the mic/recorder; waits (including seek waits) and read failures are reported. Read errors stop music and notify the UI. Music resampling remains linear. Retired-reader joins happen after output resumes.
- Release/CTest passed 3/3, adding standalone WAV streaming tests with generated 24-bit mono/stereo fixtures at 44.1/48 kHz. Tests compare samples across fractional block boundaries, forward/backward seeks and final frame, and reject non-finite positions. The expanded verifier passed (`build/evidence-20260908-214031`): `--test-playback` drives a short synthetic file through the real engine and verifies nonzero output, pause silence/position, backward/forward seek, end-of-track stop and finalized nonzero stereo recording with correct frame count/peak bounds. Existing DSP, fault-routing and UI/session checks also pass. No physical music playback, slow-storage failure injection or long-file memory profiling was performed. The earlier intermediate playback check counted four music wait blocks during a sequence including seeks; it was not a gapless-playback test.
- Lyrics follow-up: main/separate lyric views are now read-only multiline editors with scrollbars. Unchanged text is not reassigned every timer tick; changed timed lyrics return to the top. F11/Esc forwarding is implemented for the separate view, but full keyboard interaction on hardware has not been manually revalidated. TXT treats brackets literally. Reads are bounded to 256 KiB plus one detection byte; oversized/missing files are rejected before replacing current lyrics. Timestamp parsing accepts numeric minutes/seconds with up to three fractional digits and rejects seconds >=60 and malformed numeric fields. Untimed text is cached and timed-line lookup uses binary search. LRC offset metadata is still not implemented; the saved manual offset slider applies.
- Release/CTest passed 2/2; offline/UI verification passed (`build/evidence-20260908-212721`). UI smoke checks valid/malformed timestamps, read-only text, unchanged-content caret retention and reset for a new lyric. Main snapshot was visually inspected. These checks do not prove every scroll/input interaction, file-permission/size race or LRC dialect.
- Session follow-up: up to 128 queued WAV paths and the bounded lyric offset now persist in the local session, but are excluded from vocal preset exports. Restoring a queue validates types/absolute paths/extensions without accessing media, leaves selection empty and does not trigger loading/playback/monitoring. Clear queue removes descriptors only, not files or current playback. Lyric-file selection remains unimplemented. UI smoke tests round-trip missing synthetic Unicode paths and offset, reject malformed entries, bound oversized lists/offset, and verify no playback/monitoring/load was triggered. The real session file hash remained unchanged during local smoke. Main layout was visually inspected (`build/session-ui-20260908-212238.png`); Release/CTest passed 2/2. `scripts/verify.ps1` now includes no-stream UI/session/EQ smoke and its three PNGs, and passed with offline WAV/fault checks (`build/evidence-20260908-212325`). Hosted execution of this newly expanded verifier remains to be checked.
- Room follow-up: four exact Schroeder allpass sections now diffuse the summed room-comb return; they are not on the dry voice path. Fixed feedback 0.5 and sample-rate-scaled lengths 149/211/263/293 at 48 kHz; storage is allocated only during preparation. Existing room sends/bypass/presets still apply. Reference: [Julius O. Smith, Schroeder allpass sections](https://www.dsprelated.com/freebooks/pasp/Schroeder_Allpass_Sections.html). No third-party implementation was copied or added as a dependency.
- At 44.1/48 kHz, diffusion tests check impulse-energy preservation, reflection spreading, late-tail decay, state reset, finite invalid-input handling and magnitude at 100/1000/10000 Hz. An integrated vocal-DSP comparison verifies unchanged direct onset and a nonzero room tail whose late energy is below one millionth of its early energy. Release/CTest passed 2/2; offline mix/dry/wet and injected-fault verification passed (`build/evidence-20260908-211902`). This demonstrates tested mathematical behavior, not a preferred sound or hardware glitch-free performance. Room remains a basic mono algorithm, not stereo/convolution reverb.
- Service-timing follow-up: native diagnostics now distinguish a discontinuity on the very first capture packet from the total, count capture timestamp errors, and report maximum capture/render service intervals using QPC. These are software service intervals, not device latency, lost-frame counts or RTT. The fixed-storage interval tracker has deterministic first-tick/maximum/backwards-tick/reset tests. Native lifecycle tests validate the counters and passed three Realtek cycles, each with its sole discontinuity on the first packet and maximum service intervals around 10.4-10.8 ms.
- A 60-second silent run of the current EQ/recovery/instrumented build completed (`build/native-timing-20260908-211536.txt`, process 21312 exited): Realtek mic array -> headphones, 48 kHz, 480/480 periods, output queue target 528, 6000 capture packets/6000 render callbacks; one capture discontinuity, on the first packet; zero timestamp errors, FIFO under/overruns, resyncs/discarded frames and empty-padding observations. Maximum capture/render service intervals were 11.47/12.01 ms, final HRESULT 0. It ran concurrently with short lifecycle and offline tests. This short clean observation does NOT explain or invalidate the earlier 30-minute capture gaps, and does not pass the audible singing/recording gate. Build/CTest and offline WAV/fault checks passed (`build/evidence-20260908-211610`).
- Hosted Windows CI passed for `7c7cf4d`: [run 34236115759](https://github.com/tang-vu/cantodeck/actions/runs/34236115759). That run predates the parametric EQ and service-interval instrumentation.
- The earlier 30-minute silent run is now complete: `build/native-soak-20260908-204006/result.txt`, executable SHA256 `863092cc6a222612ca5b9bbacc6a78c38720f6e1b84c3b24c0c32280d8800e46`. Realtek mic array -> headphones, 48 kHz, 480/480-frame periods, queue target 528; 179567 capture packets/180000 render callbacks, 5 capture discontinuity flags, 3 FIFO underruns, 0 overruns, 0 empty-output-padding observations, final HRESULT 0. The process handle is gone and the final report records 1800 seconds. This is NOT a glitch-free pass or audible singing/recording soak. The run predates backlog recovery, bypass changes and parametric EQ; it cannot validate those changes. The cause/timing of its capture gaps was not established.
- Three bell EQ bands are now in the vocal DSP path, each with 40-16000 Hz (limited to 0.45*sampleRate), +/-12 dB gain and Q 0.2-8. Parameters are smoothed on a 64-sample control interval; fixed filter state has no allocation/locks. `Vocal EQ` bypass ramps gains toward zero, while `Giọng gốc` bypasses the entire coloration path. The `EQ 3 bands` window edits all nine parameters; JSON preset/session save/load includes them and old JSON defaults to neutral. Named presets reset EQ gains to neutral. Broad Tone remains for legacy presets. Filter formula reference: [RBJ/W3C Audio EQ Cookbook](https://www.w3.org/TR/audio-eq-cookbook/).
- EQ tests at 44.1/48 kHz cover center gain at 120/1200/6500 Hz, -12/-6/0/+6/+12 dB, three-band cascade, Q bandwidth, settled bypass and rapid parameter changes/non-finite frequency. Release/CTest and offline WAV/fault checks passed (`build/evidence-20260908-211053`). EQ JSON round-trip and UI snapshots passed (`build/eq-ui-20260908-211147-eq.png`); the EQ and updated Advanced panels were visually inspected. This does not certify all automation patterns, physical sound quality or zero latency.
- UI checkpoint: main/advanced screenshots at 1080x820 were rendered and visually inspected (`build/ui-check-20260908-210517.png` and its `-advanced` companion). Native/transparent/measurement controls and diagnostics were readable without visible overlap. Smoke mode now skips session restoration/writes, and the existing session file hash was unchanged across this run. Both image-write failures now affect the exit status. This is not full interaction/high-DPI coverage. Release/CTest passed 2/2 and offline WAV/fault verification passed again (`build/evidence-20260908-210536`).
- Native lifecycle follow-up: backend rejects missing callbacks/endpoints, non-finite/out-of-range sample rates and invalid periods before starting its worker. CTest now runs two tests, including device-independent native validation; both pass. The separate opt-in `native_backend_tests --hardware-silent` test opens streams but only writes zero output and never inspects/saves captured samples. Three open/pause/resume/close cycles passed for explicitly selected Realtek microphone array -> Realtek headphones at 48 kHz, 480/480-frame periods. No application callbacks occurred during the 200 ms paused intervals or after close; callbacks resumed afterward. Each cycle reported one capture discontinuity and zero empty-padding observations; the discontinuity cause was not established. A preceding first-endpoint run selected virtual Steam mic at 44.1 kHz -> Realtek 48 kHz and also passed; that is not a physical microphone test. These short tests ran alongside the separate silent endurance process and establish neither audibility nor RTT. Offline WAV/fault verification also passed (`build/evidence-20260908-210231`).
- Hosted Windows build/offline tests/packaging passed for checkpoint `16a4746`: [run 34234437110](https://github.com/tang-vu/cantodeck/actions/runs/34234437110). Later local commits are not covered by that hosted run.
- Bypass-transition follow-up: Release/CTest and offline mix/dry/wet verification passed (`build/evidence-20260908-205754`). At 44.1/48 kHz, a deterministic DC test switches each direction twice, bounds sample steps and verifies exact selected-path settling; invalid boost/threshold conversion parameters remain finite. This is not a listening test or a performance benchmark.
- Fault-routing follow-up: dry/wet recorder taps now obey the device-fault latch, as the mix already did. The GUI control timer finalizes an active interrupted recording once track loading is idle. Release/CTest and offline verification passed (`build/evidence-20260908-210001`): the real engine graph received nonzero synthetic input with fault latched and monitor requested on, produced silent output, then finalized/reopened 4096-frame mix/dry/wet WAVs and verified every sample was zero. This does not test Windows unplug notifications or the GUI timer under real device loss.
- Pre-publication verification on 2026-09-08: Release rebuild and CTest passed (1/1); `scripts/verify.ps1` passed actual offline graph rendering and mix/dry/wet WAV finalization (`build/evidence-20260908-204943`). Portable packaging with matching application/dependency sources and SHA256 also passed. These checks do not validate audible monitoring or physical round-trip latency.
- Deterministic checks cover transparent-waveform preservation, stereo-linked limiting/release, previous DSP/FIFO tests, asymmetric 128/480 callback drift, 10 kHz amplitude preservation under fractional drift, a known delayed calibration signal and rejection of a silent return.
- Follow-up verification: measurement tests at 44.1/48 kHz also cover inverted/DC-offset returns, two equal separated returns, clipping, muted output, unprepared/duplicate start, cancellation before/during capture and restart. Cancelled capture transitions use compare/exchange so completion cannot overwrite a concurrent cancellation. Release/CTest and offline WAV verification passed again (`build/evidence-20260908-205219`). These are deterministic checks, not a stress-test proof of all thread interleavings.
- A native probe using deliberately nonexistent input/output names exited with code 12 and `HRESULT 0x80070490`, without silently choosing another endpoint. The contemporaneous Windows list contained Realtek and Steam endpoints, not DGM20. This tests missing selections, not physical unplug during streaming.
- Backlog recovery regression fills the 32768-frame FIFO and verifies 100 rejected writes, exact consumer discard counts, recovery to the target queue, bounded DC transitions through starvation, and successful return of a new opposite-polarity source. Existing positive/negative drift and mixed-rate tests now also require zero resynchronizations, so recovery cannot mask unstable clock adaptation. Release/CTest passed; offline graph/WAV verification passed (`build/evidence-20260908-205511`). Physical glitch/recovery listening remains untested. The ongoing 30-minute run uses the earlier immutable binary and does not cover this change.
- A native silent Realtek microphone-array/headphones probe ran for 30 seconds: 48 kHz, 480-frame input/output periods, 1056-frame WASAPI capacities, 528-frame output queue target, 3000 capture packets and 3000 render callbacks. One capture discontinuity flag occurred; the initial packet may carry this flag, but its cause was not separately established. Application FIFO under/overruns and observed empty output paddings were zero. This run preceded the final sinc/calibration additions.
- A 30-minute silent native run of the newer sinc/calibration build was started at local time 20:40:06. Its separate executable is `build/native-soak-20260908-204006/CantoDeck.exe`; report target `result.txt` in that directory; process ID at launch 14164. Revalidate the live process/path or the final report before treating it as running/completed. Do not count it as an audible singing/recording soak or as DGM20 validation.

## Remaining acceptance work

Verify native device loss/reconnect, stop/start/pause, missing endpoints, RAW/fallback behavior, physical loop measurements, vocal listening quality and the real external-speaker route. Complete the remaining specification features and the hardware matrix. In particular, a successful Realtek silent stream does not establish sub-20 ms measured RTT or acceptable singing performance on DGM20/other speakers.

Native mode is currently selectable in Advanced, alongside the JUCE compatibility route. The loop measurement plays no sound until the user explicitly presses its button. Do not invoke that button from an automated hardware smoke test.

Primary API references consulted: [IAudioClient3 shared initialization](https://learn.microsoft.com/en-us/windows/win32/api/audioclient/nf-audioclient-iaudioclient3-initializesharedaudiostream), [client stream properties](https://learn.microsoft.com/en-us/windows/win32/api/audioclient/nf-audioclient-iaudioclient2-setclientproperties).
